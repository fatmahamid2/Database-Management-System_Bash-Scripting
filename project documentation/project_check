1) main menu
    check:

     if user input is not a number 
     if user input cintain a \ 
     if user input is a number not in the menu 

2) create Data Base
    check:

    if name cintain spaces: Replace spaces with underscores 
    if user input cintain a \ 
    if DB name already exist 
    if data base name starts with a number 
    if data base name contains a sepical charchters 
    If all checks pass, create the DB folder 

3) Display Data Base    
    check:

    if no databases exist 
    if any databases exist 

4) Connect to Data Base    
    check:
a)
    if no databases exist 
    if DB name doesn't exist 
    if name contain spaces: Replace spaces with underscores 
    if user input cintain a \ 

b) create table
    check:

    if name contain spaces: Replace spaces with underscores 
    if user input cintain a \ 
    if table name already exist 
    if table name starts with a number 
    if table name contains a sepical charchters 
    If all checks pass, create the table file 

c) insert into table
    check:

    if name contain spaces: Replace spaces with underscores 
    if user input cintain a \ 
    if table name doesn't exist 
    if table name starts with a number 
    if table name contains a sepical charchters 
    If all checks pass, create the table file 
    If it's a PK, ensure at least one value is entered 
    if the PK value already exists  
    Validate data type 








